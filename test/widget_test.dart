import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder test', (WidgetTester tester) async {
    // TODO: 実機テスト中心のため、ユニットテストは後続で追加
    expect(1 + 1, equals(2));
  });
}
